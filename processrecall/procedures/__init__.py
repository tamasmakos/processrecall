"""procedures — what an event *means*: steps, their classes, their outcomes.

The symbolic labelling of raw activity. An event becomes an ordered sequence of
steps, each named by a procedure class at three levels of generality and given
an outcome. This is where a command string stops being a string.

On the hot path, so stdlib only.
"""
